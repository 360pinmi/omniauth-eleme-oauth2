require "spec_helper"

describe Omniauth::Eleme::Oauth2 do
  it "has a version number" do
    expect(Omniauth::Eleme::Oauth2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
